# Generated by map/tools/update_georef.py — edit map/georef.yaml instead.
gps_to_map:
  ros__parameters:
    datum_lat: 38.6435
    datum_lon: -90.3161244
    # map-frame position of the datum (matches the map yaml origin offset,
    # negated: a +east origin shift means the datum sits at -east in map frame)
    datum_x: -0.0
    datum_y: -0.0
